# New HTML Project

This project contains a basic HTML page with linked CSS and JavaScript files.

- `index.html`: Main HTML file
- `style.css`: Basic styling
- `script.js`: Simple JS interactivity

Open `index.html` in your browser to view the page.